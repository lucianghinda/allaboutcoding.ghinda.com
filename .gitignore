# Secrets — never commit
bin/.hashnode_token
.env

# Unpublished drafts — kept locally, not committed (would be public if the repo is).
# Un-ignore these two lines if your GitHub repo is private and you want drafts in git.
_drafts/
bin/data/hashnode_drafts.json

# Jekyll build output & caches
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler
.bundle/
vendor/bundle/

# OS / editor
.DS_Store
*.swp
